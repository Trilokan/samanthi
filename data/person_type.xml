<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Person Type : Doctor -->
    <record id="person_type_doctor" model="person.type">
        <field name="name">Doctor</field>
    </record>

    <!-- Person Type : Driver -->
    <record id="person_type_driver" model="person.type">
        <field name="name">Driver</field>
    </record>

    <!-- Person Type : Nurse -->
    <record id="person_type_nurse" model="person.type">
        <field name="name">Nurse</field>
    </record>

    <!-- Person Type : Staff -->
    <record id="person_type_staff" model="person.type">
        <field name="name">Staff</field>
    </record>

    <!-- Person Type : Patient -->
    <record id="person_type_patient" model="person.type">
        <field name="name">Patient</field>
    </record>

    <!-- Person Type : Service -->
    <record id="person_type_service" model="person.type">
        <field name="name">Service</field>
    </record>

    <!-- Person Type : Suppier -->
    <record id="person_type_supplier" model="person.type">
        <field name="name">Supplier</field>
    </record>


</odoo>